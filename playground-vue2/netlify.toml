[build]
publish = "dist"
command = "npx pnpm i && pnpm --filter markstream-vue2 build && npm run build"

[build.environment]
NPM_FLAGS = "--version"
NODE_VERSION = "22"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
